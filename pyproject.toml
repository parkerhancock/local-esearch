[project]
name = "local-esearch"
version = "0.1.0"
description = "Elasticsearch-compatible API backed by SQLite + FTS5 + sqlite-vec"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "google-genai>=1.59.0",
    "sqlite-vec>=0.1.6",
]

[project.optional-dependencies]
voyage = ["voyageai>=0.2.0"]
gemini = ["google-genai>=1.0.0"]
openai = ["openai>=1.0.0"]
vector = ["sqlite-vec>=0.1.0"]
postgres = ["psycopg[binary]>=3.0.0", "pgvector>=0.2.0"]
all = [
    "voyageai>=0.2.0",
    "google-genai>=1.0.0",
    "openai>=1.0.0",
    "sqlite-vec>=0.1.0",
    "psycopg[binary]>=3.0.0",
    "pgvector>=0.2.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/local_esearch"]

[dependency-groups]
dev = [
    "pgvector>=0.2.0",
    "psycopg[binary]>=3.0.0",
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "pytest-cov>=7.0.0",
    "sqlite-vec>=0.1.0",
]
